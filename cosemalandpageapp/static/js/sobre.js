document.addEventListener('DOMContentLoaded', function () {
    // Inicializa AOS com configuração de reset
    AOS.init({
        duration: 800,       // duração da animação
        once: false,         // anima mais de uma vez
        mirror: true         // anima novamente ao subir
    });
});
